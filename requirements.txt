# TODO: Update with exact module version
numpy<2.0      # Keep <2.0 for better compatibility with older ByteTrack code
torch>=2.0.0
torchvision>=0.15.0
opencv-python
loguru
scikit-image
tqdm
Pillow
thop
ninja
tabulate
tensorboard
lapx           # Use lapx for better Windows/Python 3.12 compatibility
motmetrics
filterpy
h5py
cython         # Needed for some extensions
pycocotools    # Needed for COCO evaluation and data loading

# verified versions for Python 3.12
onnx>=1.15.0
onnxruntime>=1.16.0
onnxsim>=0.4.3

gdown 
kagglehub